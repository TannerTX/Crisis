const client = require("../index.js")
const { EmbedBuilder } = require('discord.js')

module.exports = {
    name: 'botinfo',
    description: 'Displays all Bot Infomation',
    usage: ',botinfo',
    execute(message) {

        
    }

}